<div class="b2b-side-nav-mask">
</div>

<div class="b2b-side-nav-btn">
  <i class="fa fa-bars" aria-hidden="true"></i>
</div>

<aside class="b2b-side-nav">
  <div class="logo-text" style="background-image: url(<%- layoutImages.indexLogo.url %>)"></div>
  <ul class="b2b-side-nav-list">
    <% var regx = /category=([0-9]+)&/gi; %>
    <% var category = regx.exec( req.url ); %>
    <% for(var i = 0; i < data.categorys.length; i++) { %>
      <% if( req.url === '/' && data.categorys[i].id  === 1 ){ %>
        <li><a class="active" href="product?start=0&length=10&category=<%= data.categorys[i].id %>&limit=false"><%= data.categorys[i].CategoryDescription.name %></a></li>
      <% } else if ( category && category[1] == data.categorys[i].id ) { %>
        <li><a class="active" href="product?start=0&length=10&category=<%= data.categorys[i].id %>&limit=false"><%= data.categorys[i].CategoryDescription.name %></a></li>
      <% } else { %>
        <li><a href="product?start=0&length=10&category=<%= data.categorys[i].id %>&limit=false"><%= data.categorys[i].CategoryDescription.name %></a></li>
      <% }%>
    <% } %>
  </ul>
</aside>


<% LayoutUtils.addScriptBlock(`
  $(document).ready(function(){
    // $('.b2b-side-nav').css({
    //   height: $('body').height(),
    // })
    // $(window).resize(function(){
    //   $('.b2b-side-nav').css({
    //     height: $('body').height(),
    //   })
    // });
    $('.b2b-side-nav-btn').click(function(){
      $('.b2b-side-nav, .b2b-side-nav-mask').addClass('open');
    });
    $('.b2b-side-nav-mask').click(function(){
      $('.b2b-side-nav, .b2b-side-nav-mask').removeClass('open');
    });
  });
`); %>
